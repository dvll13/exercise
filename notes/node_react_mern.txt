ENTER NODE CONSOLE
------------------
type `node` in the normal console

you have top level `global` and `process` objects

arrow functions don't have their own context (`this` points to the upper context)
